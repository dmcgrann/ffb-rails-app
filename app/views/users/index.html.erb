
<ol>
<% User.all.each do |user| %>
  <li><%= user.name %>
    <% if user.admin %>
      <%= "- ADMIN" %><br>
    <%end%>
    <%if current_user.admin%>
      <%= link_to "Delete", user, method: :delete, data: { confirm: "Really?" } %>
    <%end%>
<%end%>
</ol>
